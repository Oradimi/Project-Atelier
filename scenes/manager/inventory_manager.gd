extends Node

@export var item_list: Array[ItemData]

# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	pass
	#var mushroom: Item = Item.create(GameManager.mushroom_data)
	#item_list.append(mushroom.data)
	#var mushroom2: Item = Item.create(GameManager.mushroom_data)
	#item_list.append(mushroom2.data)
	#var honey: Item = Item.create(GameManager.honey_data)
	#item_list.append(honey.data)
	#var woodStick: Item = Item.create(GameManager.wood_stick_data)
	#item_list.append(woodStick.data)

# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	pass
